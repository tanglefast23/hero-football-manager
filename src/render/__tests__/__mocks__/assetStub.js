// Jest stub for binary media assets (*.wav / *.m4a) — see jest.config.js
// moduleNameMapper. The real numeric module id Metro assigns is irrelevant to
// the pure logic under test (audio.ts's filesForEvent mapping).
module.exports = 0;
